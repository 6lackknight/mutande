# Weekly pilot / waitlist — week of 2026-08-18

- **Date:** 2026-08-18 (Tue)
- **Window:** ISO week 34. Monday note is the still-unmerged `2026-08-17-weekly.md` (PR #12). This file is the mid-week rollup plus overnight **2.0.4–2.0.7** product context.
- **Automation:** `mutande-growth-strat` (daily cron; this file is the weekly note)
- **Pilot progress:** **1/5** (Roy, 2026-08-12)
- **Keep-open (n=1):** yes
- **Lead wedge:** still **A + C** (cofounder story + dev/MCP credibility)

This is a post-pilot week note, not a new friend call. Copy `../TEMPLATE.md` for pilots 2–5.

Mon (PR #12), Sun (PR #9), and Fri/Sat drafts (PRs #7 / #8) are still unmerged. Main still has no weekly file. No owner reply landed on those PRs (Vercel bot only).

---

## Please provide (blocked this run)

Hub `GET /health` is **ok**. Live waitlist, in-app feedback, and Mixpanel Query are still **not available** to the automation. **Do not treat empty cells as zero usage.**

| Source | What happened |
|--------|----------------|
| Hub `/v1/admin/waitlist` + `/v1/admin/feedback` | `MUTANDE_AUTH0_ACCESS_TOKEN` is still a 1-character placeholder. This run: hub **401** `Invalid or expired token` on both admin paths (not the Cloudflare **1010** from 2026-08-17). Health stays 200. |
| Mixpanel Insights / JQL | No service account in env (`MIXPANEL_SERVICE_ACCOUNT_USERNAME` / `SECRET` / `MIXPANEL_PROJECT_ID` unset). Client project token cannot read counts. |
| Website Mixpanel (`surface=web`) | Instrumented; live 7d/30d uniques + totals missing. |
| Desktop / “the app” Mixpanel (`surface=desktop`) | Shipped in **1.1.4 (2026-08-13)**. Coverage is now **~5 days**. Overnight **2.0.4–2.0.7 (Aug 17)** means `app_version` may split 1.1.4 / 2.0.0 / 2.0.1 / 2.0.4+. New event: `desktop_update_required`. |
| iOS / mobile Mixpanel | **Not shipped** (v1.5). Flutter registers `surface=desktop`. There is no `surface=mobile` / `ios_*` tracker in this repo. Feedback `platform` already includes `ios`. |

**Reply on this PR** (no emails, handles, or tokens) with whatever you have:

1. **Waitlist** from `/admin/ops`: total n, **last 7 days** (and last 30 if easy), OS / hosts / share frequency / share methods, multi-host %, company-domain **count** (not the domains). Confirm whether anyone joined **Aug 9–18**.
2. **In-app feedback** since 2026-08-10: count, platform (`macos` / `ios` / `web`), category, short quotes.
3. **Mixpanel website** (7d and 30d): uniques + totals for `$mp_web_page_view`, `waitlist_click`, `download_nav_click` (break down `surface`: `landing_hero` / `site_nav` / `dashboard_cta`), `download_artifact_click` (break down `platform` + `surface` — see download-picker change below), `sign_in_click`, `create_account_click`.
4. **Mixpanel desktop / Mac+Windows app** (7d from Aug 13): uniques + totals for the 1.1.4 funnel below, split `platform=macos|windows` and `app_version` (`1.1.4` vs `2.0.0` / `2.0.1` / `2.0.4`–`2.0.7`). Include `desktop_tab_select` with `tab=collab` and the new `desktop_update_required` (`current` / `latest`).
5. **Mixpanel mobile / iOS:** if a separate consumer iOS project exists, send the **project id** + a 7d Insights export (uniques/totals). Nothing in-repo emits `ios_*` / `surface=mobile`. If “mobile” meant the Flutter build on a laptop, use `surface=desktop`.
6. **Pilots 2–5** if any happened off-repo (date, keep-open, blockers).
7. **Invite / install reality:** how many waitlist people were actually invited or installed since Aug 10.

For the next automated run, add env secrets: `MIXPANEL_SERVICE_ACCOUNT_USERNAME`, `MIXPANEL_SERVICE_ACCOUNT_SECRET`, `MIXPANEL_PROJECT_ID` (plus optional `MIXPANEL_IOS_PROJECT_ID` if iOS is separate), and a working SuperAdmin hub token (`MUTANDE_AUTH0_ACCESS_TOKEN`). A redacted `/admin/ops` paste on this PR is the fallback if the runner still cannot read admin.

---

## Pilot

| Friend | Date | Self-collab | Ping Tawanda | Keep open | Notes |
|--------|------|-------------|--------------|-----------|--------|
| Roy Nyamhute | 2026-08-12 | pass | pass | **yes** | MCP walkthrough, notifications, Keychain; ~80% value from site; product exceeded expectations |

**No new session files** since Roy. Gate before public LinkedIn still holds: finish 5 pilots + ≥2-people blocker sprint.

**Blockers still n=1 (do not sprint yet):** MCP connect ceremony, notification opt-in, Keychain/device register, UI polish (logo / permission prompts). Logo shipped in 1.1.1–1.1.2; MCP/onboarding remains the launch gate. Next pilots should use the current connect flow + Collab 2.0 chrome + the non-blocking update check (2.0.6 / 2.0.7), not the Roy-call screenshots.

**Would-use (Roy):** stop tab-switching / copy-paste; agents cross-check; cheaper than multi-host team seats; file/PR handoff; “email for AI” addressing. Enterprise/local-model came up **on the call**, not on the waitlist form.

---

## Waitlist (last known: 2026-08-13, n=9)

Source: `pilot/pilot-direction.md` export from `/admin/ops`. Entries dated **Aug 4–8**. This run could not confirm whether anyone joined Aug 9–18.

| Metric | n=9 | n=8 excl. founder |
|--------|-----|-------------------|
| Share frequency | 6× multiple/day · 1× few/week · 1× occasionally · 1× rarely/never | 5× multiple/day · 1× each other |
| OS | 6 macOS · 3 Windows | 5 macOS · 3 Windows |
| Multi-host (≥2) | 6 (67%) | 5 (63%) |
| Copy / paste | 8 (89%) | 7 (88%) |
| Cloud drive | 5 | 5 |
| Local files / notes | 4 | 4 |
| Email | 2 | 2 |
| Slack / chat | 0 | 0 |

**Top hosts:** Claude Desktop 6 · Claude Code 4 · ChatGPT desktop 4 · Gemini 4 · Cursor 2 · Copilot 1 · ChatGPT web 1.

**Read:** demand still looks like **A + C**. Windows is 33% of the list while Mac remains the invite batch. Slack/chat is still zero — pain is copy/paste and files, not “another chat app.”

**Unknown vs last export:** last-7-day waitlist count (snapshot implied **0** after Aug 8; needs confirmation). Site still gates Try Alpha behind the waitlist form (`/waitlist?next=/download` + cookie `mutande_alpha_ok`), so new completions — if any — should show up on hub even when Mixpanel only saw a click.

---

## Mixpanel — instrumented vs live

**Live stats: unavailable** (see ask above). Same Mixpanel project for web + desktop; stitch on Auth0 `sub` only (no email/handle in props). Web: autocapture + 10% session replay. Desktop 1.1.4+: `trackAutomaticEvents: false`.

### Website (`surface=web`)

| Event | Where | Why it matters |
|-------|--------|----------------|
| `$mp_web_page_view` / autocapture | sitewide after idle init | Traffic vs waitlist/download intent |
| `waitlist_click` | landing hero (`surface=landing_hero`) → `/waitlist` | Survey-only interest (Join waitlist) |
| `download_nav_click` | nav (`site_nav`), hero (`landing_hero`), dashboard (`dashboard_cta`) → `/waitlist?next=/download` | Try Alpha hits the **waitlist form first**, then `/download` after cookie unlock |
| `download_artifact_click` | `/download` picker | `platform` (`mac_arm64` / Intel / Windows) + `surface` (see 2.0.5 change) |
| `sign_in_click` | header (`site_nav`), `/login` | Account vs anonymous browse |
| `create_account_click` | `/login` | Signup vs waitlist-only |

**Gap:** the waitlist **form submit** is still not a Mixpanel event — only the hero/nav clicks are. Completions live on hub `POST /v1/waitlist`. A redacted ops snapshot is the only way to corroborate click → signup → download unlock.

**Download picker change (2.0.5, Aug 17):** `/download` now shows platform choices **before** starting a download. `download_artifact_click` `surface` values in current code are `platform_picker` (primary buttons), `platform_switch` (need a different build), and `filename` (filename links). Older Insights rows may still say `autodetect`. Do not compare this week’s `surface` mix to last week without that caveat.

**Corroboration (once numbers exist):**

- `waitlist_click` vs `download_nav_click` → survey-only vs people who already want the DMG. After the download gate, **both** paths can create a hub waitlist row.
- `download_nav_click` ≫ `download_artifact_click` → drop-off on the five-question form (or cookie already set and they bounced before the picker).
- `download_artifact_click` `platform=windows` share vs waitlist Windows 33% → are Windows signups grabbing the installer (1.1.3 sign-in fix, now 2.0.7)?
- `platform_picker` vs `platform_switch` vs `filename`: if most downloads are `platform_picker` on the suggested OS, the chooser is doing its job.
- `sign_in_click` / `create_account_click` vs waitlist n → how many browsers become accounts vs stay on the survey.

### Desktop / Mac+Windows app (`surface=desktop`, `platform=macos|windows`)

There is no separate mobile Mixpanel surface in this repo. The Flutter app registers `surface=desktop`. Use this section for laptop/desktop usage. People may say “the mobile app” when they mean the phone companion — **phone is not emitting yet**.

Onboarding funnel (no mail content; unchanged in 2.0.x except the new update event):

`desktop_app_open` → `desktop_sign_in_*` → `desktop_create_team_*` / `desktop_join_invite_*` → `desktop_connect_host_picked` → `desktop_connect_mcp_*` / `desktop_connect_skill_*` → `desktop_connect_complete` → `desktop_ping_{copy,waiting,success,skip,timeout}` → `desktop_home_ready` / `desktop_tab_select`

Plus **`desktop_update_required`** (`current`, `latest`) when the published-alpha gate fires. 2.0.4 prompted a reinstall when behind; 2.0.5–2.0.6 stopped that check from hanging startup; 2.0.7 disabled the blocking gate, then the tree restored a **non-blocking** gate. A spike here on `current=1.1.4` or `2.0.0` would mean people are still on pre-Collab / pre-picker builds.

`desktop_tab_select` values: `threads` · `collab` · `network`. Collab 2.0 (dashboard, dossiers, brain pane) did **not** add new event names — `tab=collab` is the only in-repo signal that people opened that surface.

**Corroboration:**

- Roy’s MCP friction → watch `desktop_connect_mcp_failure` vs success; if failures cluster, C (Show HN) and any public launch stay blocked.
- Ping wizard as day-one success → `desktop_ping_success` vs `skip` / `timeout`.
- Waitlist “multiple/day multi-host” → after identify, do the same people `home_ready` more than once in 7d?
- Windows waitlist vs `desktop_app_open` `platform=windows` after 1.1.3 / 2.0.7.
- Did anyone update after the Aug 17 cuts? Split `app_version` 1.1.4 vs 2.0.0 / 2.0.1 vs 2.0.4–2.0.7. A jump in `tab=collab` only on 2.0.x would corroborate that Collab 2.0 is being seen.
- `desktop_update_required` vs later `desktop_app_open` on a newer `app_version` → did the prompt actually move people.

**Coverage caveat:** Mixpanel desktop only exists from **1.1.4 (Aug 13)**. Do not treat a quiet Insights view as no usage.

### Mobile app (iOS companion)

iOS is **v1.5** and has **no Mixpanel events** in this repo. Feedback types already allow `platform=ios`, so in-app feedback is the only in-repo mobile signal until a tracker ships.

If a separate consumer iOS Mixpanel project exists, send the project id + a 7d Insights export so the next run can corroborate website vs phone vs desktop. Until then, **website + desktop** are the only instrumented surfaces.

---

## Product shipped (context for behavior)

- **1.0.11–1.0.12 (Aug 7):** crash reporting, Windows installer, app/core version match.
- **1.1.0 (Aug 11):** Network tab, external contacts + PIN, compose in toolbar.
- **1.1.1–1.1.2 (Aug 11–12):** `@i` mark, multi-device send reliability (Roy called out UI polish).
- **1.1.3 (Aug 12):** Windows Auth0 sign-in URL fix — relevant to the 3 Windows waitlist rows.
- **1.1.4 (Aug 13):** Network inspect + mail split; **desktop Mixpanel funnel**; web identify stitch.
- **2.0.0 (Aug 16):** Collab tab — boards, steerers, standing instructions; MCP list/open/move.
- **2.0.1 (Aug 16):** Collab on Windows + sync stability.
- **2.0.4 (Aug 17):** Collab home dashboard (dossiers, activity, shared brain); manage members/roster/archive; notifications panel; reinstall prompt when behind published alpha.
- **2.0.5 (Aug 17):** Startup no longer hangs if mutande.online is unreachable; **download page shows platform choices before starting**.
- **2.0.6 (Aug 17):** Update check is non-blocking — app opens immediately.
- **2.0.7 (Aug 17):** Disable blocking startup update gate; tree then restored a non-blocking gate. Site download version is **2.0.7**.

Next pilots will not see the Roy-call chrome. Desktop Mixpanel `app_version` should read **2.0.7** for anyone who installed after the latest cut; older installs stay on 1.1.4 / 2.0.0–2.0.1 until they update.

---

## Inferred behavior (and what Mixpanel must confirm)

| Inference | From | Confirm with |
|-----------|------|----------------|
| Pain is copy/paste + multi-host, not Slack | Waitlist methods; Roy “tab loop” | Web waitlist vs download; desktop ping + tab use after install |
| Mac-first invite batch is right | 67% macOS waitlist; v1 Mac-complete | `download_artifact_click` + `desktop_app_open` by platform |
| Windows demand is real but product-lagging | 3 Windows signups; installer + sign-in landed | Windows download and `desktop_sign_in_success` on 1.1.3+ / 2.0.7 |
| MCP is the public-launch gate | Roy n=1 | `desktop_connect_mcp_*` (need more than founder/Roy) |
| Waitlist may have gone quiet after Aug 8 | Snapshot date range | Ops last-7-day waitlist + `waitlist_click` / `download_nav_click` 7d |
| Try Alpha now *is* a waitlist event | `/waitlist?next=/download` gate | Hub completions vs those two click events; form submit still untracked |
| Collab 2.0 is a new desktop surface, not a new funnel | 2.0.0–2.0.4 notes; no new collab event names | `desktop_tab_select` `tab=collab` on `app_version` 2.0.x |
| Stale installs may be stuck pre-2.0.7 | 2.0.4–2.0.7 update-gate churn | `desktop_update_required` + later `app_version` |
| Download chooser replaced silent autodetect | 2.0.5 picker-first UI | `download_artifact_click` `surface=platform_picker` vs older `autodetect` |
| Enterprise is a call topic, not a form segment | Roy; no compliance fields | Do not overweight B until pilots 2–5 or a design-partner ask |
| Mobile usage cannot be corroborated yet | No iOS Mixpanel; feedback `platform` includes `ios` | Ship iOS Mixpanel or export a separate project |

**Strategy score (unchanged without new waitlist):** A high · C high · B low. Stay **A + C**. Do not LinkedIn-blast yet.

---

## Checklist

- [ ] Owner: redacted waitlist + feedback snapshot on this PR (PRs #7/#8/#9/#12 can close once this lands)
- [ ] Owner: Mixpanel 7d/30d for **website** + **desktop app** events (or Query secrets for next run)
- [ ] Owner: confirm whether any iOS / `surface=mobile` Mixpanel project exists — send project id + 7d Insights if yes
- [ ] Pilots 2–5 scheduled / filed under `pilot/sessions/YYYY-MM-DD-firstname.md`
- [ ] ≥2-people blocker sprint still waiting on a second MCP/onboarding fail
- [ ] Batch invites still: **macOS + ≥2 hosts + multiple/day** first; hold Windows until those people are in
- [ ] Roy landing-page workflow clip still open
