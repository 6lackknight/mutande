# Weekly pilot / waitlist — week ending 2026-08-14

- **Date:** 2026-08-14 (Fri)
- **Window:** 2026-08-07 → 2026-08-14
- **Automation:** `mutande-growth-strat` (daily cron; this file is the weekly note)
- **Pilot progress:** **1/5** (Roy, 2026-08-12)
- **Keep-open (n=1):** yes
- **Lead wedge:** still **A + C** (cofounder story + dev/MCP credibility)

This is a post-pilot week note, not a new friend call. Copy `../TEMPLATE.md` for pilots 2–5.

---

## Please provide (blocked this run)

Live ops and Mixpanel Query were **not available** from the cloud agent:

| Source | What happened |
|--------|----------------|
| Hub `/v1/admin/waitlist` + `/v1/admin/feedback` | `MUTANDE_AUTH0_ACCESS_TOKEN` is present but empty; Cloudflare **1010** also blocks this egress to `hub.mutande.online` |
| Mixpanel Insights / JQL | No service account in env. Client project token cannot read counts |
| iOS / mobile Mixpanel | **Not shipped** (v1.5). Desktop Flutter uses `surface=desktop` |

Reply on the PR (no emails, handles, or tokens) with whatever you have:

1. **Waitlist** from `/admin/ops`: total n, **last 7 days**, OS / hosts / share frequency / share methods, multi-host %, company-domain count (not the domains).
2. **In-app feedback** since 2026-08-07: count, platform, category, short quotes.
3. **Mixpanel** (web + desktop, 7d and 30d): uniques + totals for the events below. A CSV / Insights screenshot is enough until Query creds land.
4. **Pilots 2–5** if any happened off-repo (date, keep-open, blockers).
5. **Invite / install reality:** how many waitlist people were actually invited or installed this week.

For the next automated run, add env secrets: `MIXPANEL_SERVICE_ACCOUNT_USERNAME`, `MIXPANEL_SERVICE_ACCOUNT_SECRET`, `MIXPANEL_PROJECT_ID`. A working SuperAdmin hub token only helps if this environment can reach the hub (today it cannot).

---

## Pilot

| Friend | Date | Self-collab | Ping Tawanda | Keep open | Notes |
|--------|------|-------------|--------------|-----------|--------|
| Roy Nyamhute | 2026-08-12 | pass | pass | **yes** | MCP walkthrough, notifications, Keychain; ~80% value from site; product exceeded expectations |

**No new session files this week** besides Roy (already filed). Gate before public LinkedIn still holds: finish 5 pilots + ≥2-people blocker sprint.

**Blockers still n=1 (do not sprint yet):** MCP connect ceremony, notification opt-in, Keychain/device register, UI polish (logo / permission prompts). Logo shipped in 1.1.1–1.1.2; MCP/onboarding remains the launch gate.

**Would-use (Roy):** stop tab-switching / copy-paste; agents cross-check; cheaper than multi-host team seats; file/PR handoff; “email for AI” addressing. Enterprise/local-model came up **on the call**, not on the waitlist form.

---

## Waitlist (last known: 2026-08-13, n=9)

Source: `pilot/pilot-direction.md` export from `/admin/ops`. Entries dated **Aug 4–8**. This run could not confirm whether anyone joined Aug 9–14.

| Metric | n=9 | n=8 excl. founder |
|--------|-----|-------------------|
| Share frequency | 6× multiple/day · 1× few/week · 1× occasionally · 1× rarely/never | 5× multiple/day · 1× each other |
| OS | 6 macOS · 3 Windows | 5 macOS · 3 Windows |
| Multi-host (≥2) | 6 (67%) | 5 (63%) |
| Copy / paste | 8 (89%) | 7 (88%) |
| Cloud drive | 5 | 5 |
| Local files / notes | 4 | 4 |
| Email | 2 | 2 |
| Slack / chat | 0 | 0 |

**Top hosts:** Claude Desktop 6 · Claude Code 4 · ChatGPT desktop 4 · Gemini 4 · Cursor 2 · Copilot 1 · ChatGPT web 1.

**Read:** demand still looks like **A + C**. Windows is 33% of the list while Mac remains the invite batch. Slack/chat is still zero — pain is copy/paste and files, not “another chat app.”

**Unknown vs last week:** last-7-day waitlist count (snapshot implied **0** after Aug 8; needs confirmation).

---

## Mixpanel — instrumented vs live

**Live stats: unavailable** (see ask above). Below is what shipped, what to pull, and how it should corroborate waitlist/pilot inference. Same Mixpanel project; stitch web ↔ desktop on Auth0 `sub` only (no email/handle in props). Autocapture + 10% session replay on web.

### Website (`surface=web`)

| Event | Where | Why it matters |
|-------|--------|----------------|
| `$mp_web_page_view` / autocapture | sitewide after idle init | Traffic vs waitlist/download intent |
| `waitlist_click` | landing hero | Interest in survey vs install |
| `download_nav_click` | nav / hero / dashboard | Path to Try Alpha |
| `download_artifact_click` | `/download` picker | `platform` + `surface` (`autodetect` vs picker) — Silicon / Intel / Windows |
| `sign_in_click` | header, `/login` | Account vs anonymous browse |
| `create_account_click` | `/login` | Signup vs waitlist-only |

**Corroboration (once numbers exist):**

- `waitlist_click` ≫ `download_artifact_click` → people still signaling, not installing (matches private-alpha gate).
- `download_artifact_click` `platform=windows` share vs waitlist Windows 33% → are Windows signups actually grabbing the new installer (1.0.11+ / 1.1.3 sign-in fix)?
- Autodetect vs manual picker: if most downloads are `surface=autodetect`, the OS/arch picker is doing its job.

### Desktop app (`surface=desktop`, `platform=macos|windows`) — alpha **1.1.4** (2026-08-13)

Onboarding funnel (no mail content):

`desktop_app_open` → `desktop_sign_in_*` → `desktop_create_team_*` / `desktop_join_invite_*` → `desktop_connect_host_picked` → `desktop_connect_mcp_*` / `desktop_connect_skill_*` → `desktop_connect_complete` → `desktop_ping_{copy,waiting,success,skip,timeout}` → `desktop_home_ready` / `desktop_tab_select`

**Corroboration:**

- Roy’s MCP friction → watch `desktop_connect_mcp_failure` vs success; if failures cluster, C (Show HN) and any public launch stay blocked.
- Ping wizard as day-one success → `desktop_ping_success` vs `skip` / `timeout`.
- Waitlist “multiple/day multi-host” → after identify, do the same people `home_ready` more than once in 7d?
- Windows waitlist vs `desktop_app_open` `platform=windows` after 1.1.3.

**Coverage caveat:** Mixpanel desktop only exists from **1.1.4 (Aug 13)**. This week’s funnel is at most ~1 day of data. Do not treat a quiet week as no usage.

### Mobile app

iOS companion is **v1.5** and has **no Mixpanel events** in this repo. There is no `surface=mobile` / `ios_*` tracker to query. If “mobile” meant the Flutter desktop build, use `surface=desktop` + `platform`. If there is a separate consumer mobile Mixpanel project, send the project id + a 7d Insights export.

---

## Product shipped this window (context for behavior)

- **1.0.11–1.0.12 (Aug 7):** crash reporting, Windows installer, app/core version match.
- **1.1.0 (Aug 11):** Network tab, external contacts + PIN, compose in toolbar.
- **1.1.1–1.1.2 (Aug 11–12):** `@i` mark, multi-device send reliability (Roy called out UI polish).
- **1.1.3 (Aug 12):** Windows Auth0 sign-in URL fix — relevant to the 3 Windows waitlist rows.
- **1.1.4 (Aug 13):** Network inspect + mail split; **desktop Mixpanel funnel**; web identify stitch.
- Hosted MCP docs/tools, attachments, upvotes — supports strategy C when onboarding is boring.

---

## Inferred behavior (and what Mixpanel must confirm)

| Inference | From | Confirm with |
|-----------|------|----------------|
| Pain is copy/paste + multi-host, not Slack | Waitlist methods; Roy “tab loop” | Web waitlist vs download; desktop ping + tab use after install |
| Mac-first invite batch is right | 67% macOS waitlist; v1 Mac-complete | `download_artifact_click` + `desktop_app_open` by platform |
| Windows demand is real but product-lagging | 3 Windows signups; installer + sign-in just landed | Windows download and `desktop_sign_in_success` after 1.1.3 |
| MCP is the public-launch gate | Roy n=1 | `desktop_connect_mcp_*` (need more than founder/Roy) |
| Waitlist may have gone quiet after Aug 8 | Snapshot date range | Ops last-7-day waitlist + `waitlist_click` 7d |
| Enterprise is a call topic, not a form segment | Roy; no compliance fields | Do not overweight B until pilots 2–5 or a design-partner ask |

**Strategy score (unchanged without new waitlist):** A high · C high · B low. Stay **A + C**. Do not LinkedIn-blast yet.

---

## Checklist

- [ ] Owner: redacted waitlist + feedback snapshot on this PR
- [ ] Owner: Mixpanel 7d/30d for web + desktop events (or Query secrets for next run)
- [ ] Pilots 2–5 scheduled / filed under `pilot/sessions/YYYY-MM-DD-firstname.md`
- [ ] ≥2-people blocker sprint still waiting on a second MCP/onboarding fail
- [ ] Batch invites still: **macOS + ≥2 hosts + multiple/day** first; hold Windows until those people are in
- [ ] Roy landing-page workflow clip still open
