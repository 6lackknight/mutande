# Weekly pilot / waitlist — week of 2026-08-22

- **Date:** 2026-08-22 (Sat)
- **Window:** ISO week 34 (Mon 17 – Sun 23). Saturday rollup; Sunday is the week close. Published alpha is still **2.0.10** (changelog 2026-08-20).
- **Automation:** `mutande-growth-strat` (daily cron; this file is the weekly note)
- **Pilot progress:** **1/5** (Roy, 2026-08-12)
- **Keep-open (n=1):** yes
- **Lead wedge:** still **A + C** (cofounder story + dev/MCP credibility)

This is a post-pilot week note, not a new friend call. Copy `../TEMPLATE.md` for pilots 2–5.

Fri (PR #17), Thu (PR #16), Wed (PR #15), Tue (PR #14), Mon (PR #12), Sun (PR #9), and Fri/Sat drafts (PRs #7 / #8) are still unmerged. Main still has no weekly file. No owner reply landed on those PRs (Vercel bot only). **Please reply here with waitlist + Mixpanel aggregates** — the automation cannot invent them.

**New this run (public site, not Mixpanel):** prod `/download` now shows **Mac Silicon, Mac Intel, and Windows** (`macIntelPublished` + `winZipPublished` both true; labels `mutande 2.0.10`). The waitlist Windows-hold can lift for people who accept an unsigned SmartScreen installer. Still do **not** treat that as “Windows is ready for a blast.”

---

## Please provide (blocked this run)

Hub `GET /health` is **ok**. Live waitlist, in-app feedback, ops census, and Mixpanel Query are still **not available** to the automation. **Do not treat empty cells as zero usage.**

| Source | What happened |
|--------|----------------|
| Hub `/v1/admin/waitlist` + `/v1/admin/feedback` | `MUTANDE_AUTH0_ACCESS_TOKEN` is still a 1-character placeholder. This run: hub **401** `Invalid or expired token` on both admin paths (same as 2026-08-18 – 08-21; not the Cloudflare **1010** from 2026-08-17). Health stays 200. |
| Hub `/v1/admin/census` | Same 401. `/admin/ops` Overview already has the numbers (users, multi-host, replied threads, 7d/30d active, traffic-web bias). Counts only — **do not paste graph labels** (those are handles). |
| Mixpanel Insights / JQL | No service account in env (`MIXPANEL_SERVICE_ACCOUNT_USERNAME` / `SECRET` / `MIXPANEL_PROJECT_ID` unset). Client project token cannot read counts. **Do not invent Mixpanel numbers.** |
| Website Mixpanel (`surface=web`) | Instrumented; live 7d/30d uniques + totals missing. Needed to corroborate waitlist quiet vs site traffic. |
| Desktop / “the app” Mixpanel (`surface=desktop`) | Shipped in **1.1.4 (2026-08-13)**. Coverage is now **~9 days**. Latest **published** cut is **2.0.10**, so Insights can split that tree from **2.0.7**. Include `desktop_update_required`. First-run events are `desktop_handoff_*` (not `desktop_ping_*`). |
| iOS / mobile Mixpanel | **Not shipped** (v1.5). Flutter registers `surface=desktop`. There is no `surface=mobile` / `ios_*` tracker in this repo. Feedback `platform` already includes `ios`. |

**Reply on this PR** (no emails, handles, or tokens) with whatever you have:

1. **Waitlist** from `/admin/ops`: total n, **last 7 days** (and last 30 if easy), OS / hosts / share frequency / share methods, multi-host %, company-domain **count** (not the domains). Confirm whether anyone joined **Aug 9–22**.
2. **In-app feedback** since 2026-08-10: count, platform (`macos` / `ios` / `web`), category, short quotes.
3. **Ops census** (Overview): `users`, `orgs`, `devices`, `agents`, `multi_host_users`, `orgs_with_2plus_members`, `users_active_7d` / `users_active_30d`, `threads`, `replied_threads`, `independent_self_users`, `star_share`. Numbers only.
4. **Mixpanel website** (7d and 30d): uniques + totals for `$mp_web_page_view`, `waitlist_click`, `download_nav_click` (break down `surface`: `landing_hero` / `site_nav` / `dashboard_cta`), `download_artifact_click` (break down `platform` + `surface` — picker values below), `sign_in_click`, `create_account_click`.
5. **Mixpanel desktop / Mac+Windows app** (7d from Aug 13): uniques + totals for the funnel below, split `platform=macos|windows` and `app_version` (`1.1.4` vs `2.0.0` / `2.0.1` / `2.0.4`–`2.0.7` vs **`2.0.10`**). Include `desktop_tab_select` with `tab=collab`, `desktop_update_required` (`current` / `latest`), and **`desktop_handoff_*`**. Break `desktop_handoff_next` by `kind` (`work` / `physics` / `invite` / `skip`).
6. **Mixpanel mobile / iOS:** if a separate consumer iOS project exists, send the **project id** + a 7d Insights export (uniques/totals). Nothing in-repo emits `ios_*` / `surface=mobile`. If “mobile” meant the Flutter build on a laptop, use `surface=desktop`.
7. **Pilots 2–5** if any happened off-repo (date, keep-open, blockers).
8. **Invite / install reality:** how many waitlist people were actually invited or installed since Aug 10. Windows + Intel are **visible on prod `/download` as of this run** — say whether any of the 3 Windows waitlist rows were told.

For the next automated run, add env secrets: `MIXPANEL_SERVICE_ACCOUNT_USERNAME`, `MIXPANEL_SERVICE_ACCOUNT_SECRET`, `MIXPANEL_PROJECT_ID` (plus optional `MIXPANEL_IOS_PROJECT_ID` if iOS is separate), and a working SuperAdmin hub token (`MUTANDE_HUB_SUPERADMIN_TOKEN` or a real `MUTANDE_AUTH0_ACCESS_TOKEN`). A redacted `/admin/ops` paste on this PR is the fallback if the runner still cannot read admin.

---

## Pilot

| Friend | Date | Self-collab | Ping Tawanda | Keep open | Notes |
|--------|------|-------------|--------------|-----------|--------|
| Roy Nyamhute | 2026-08-12 | pass | pass | **yes** | MCP walkthrough, notifications, Keychain; ~80% value from site; product exceeded expectations |

**No new session files** since Roy. Gate before public LinkedIn still holds: finish 5 pilots + ≥2-people blocker sprint.

**Blockers still n=1 (do not sprint yet):** MCP connect ceremony, notification opt-in, Keychain/device register, UI polish (logo / permission prompts). Logo shipped in 1.1.1–1.1.2; MCP/onboarding remains the launch gate. Next pilots on **published 2.0.10** now see the redesigned four-step onboarding, first-job cards after handshake (`work` / `physics`), and an invite path when the org is still solo.

**Would-use (Roy):** stop tab-switching / copy-paste; agents cross-check; cheaper than multi-host team seats; file/PR handoff; “email for AI” addressing. Enterprise/local-model came up **on the call**, not on the waitlist form.

---

## Waitlist (last known: 2026-08-13, n=9)

Source: `pilot/pilot-direction.md` export from `/admin/ops`. Entries dated **Aug 4–8**. This run could not confirm whether anyone joined Aug 9–22.

| Metric | n=9 | n=8 excl. founder |
|--------|-----|-------------------|
| Share frequency | 6× multiple/day · 1× few/week · 1× occasionally · 1× rarely/never | 5× multiple/day · 1× each other |
| OS | 6 macOS · 3 Windows | 5 macOS · 3 Windows |
| Multi-host (≥2) | 6 (67%) | 5 (63%) |
| Copy / paste | 8 (89%) | 7 (88%) |
| Cloud drive | 5 | 5 |
| Local files / notes | 4 | 4 |
| Email | 2 | 2 |
| Slack / chat | 0 | 0 |

**Top hosts:** Claude Desktop 6 · Claude Code 4 · ChatGPT desktop 4 · Gemini 4 · Cursor 2 · Copilot 1 · ChatGPT web 1.

**Read:** demand still looks like **A + C**. Windows is 33% of the list. The site picker **now publishes** Windows + Intel (confirmed on prod `/download` 2026-08-22; both version **2.0.10**). Slack/chat is still zero — pain is copy/paste and files, not “another chat app.”

**Unknown vs last export:** last-7-day waitlist count (snapshot implied **0** after Aug 8; needs confirmation). Site still gates Try Alpha behind the waitlist form (`/waitlist?next=/download` + cookie `mutande_alpha_ok`), so new completions — if any — should show up on hub even when Mixpanel only saw a click.

---

## Mixpanel — instrumented vs live

**Live stats: unavailable** (see ask above). Same Mixpanel project for web + desktop; stitch on Auth0 `sub` only (no email/handle in event props). Web: autocapture + 10% session replay. Desktop 1.1.4+: `trackAutomaticEvents: false`.

Use these events to **monitor and corroborate** inferred waitlist/pilot behavior. Without Insights numbers, treat the tables as a query list, not a funnel scorecard.

### Website (`surface=web`)

| Event | Where | Why it matters |
|-------|--------|----------------|
| `$mp_web_page_view` / autocapture | sitewide after idle init | Traffic vs waitlist/download intent |
| `waitlist_click` | landing hero (`surface=landing_hero`) → `/waitlist` | Survey-only interest (Join waitlist) |
| `download_nav_click` | nav (`site_nav`), hero (`landing_hero`), dashboard (`dashboard_cta`) → `/waitlist?next=/download` | Try Alpha hits the **waitlist form first**, then `/download` after cookie unlock |
| `download_artifact_click` | `/download` picker | `platform` (`mac_arm64` / Intel / Windows) + `surface` (see picker values) |
| `sign_in_click` | header (`site_nav`), `/login` | Account vs anonymous browse |
| `create_account_click` | `/login` | Signup vs waitlist-only |

**Gap:** the waitlist **form submit** is still not a Mixpanel event — only the hero/nav clicks are. Completions live on hub `POST /v1/waitlist`. A redacted ops snapshot is the only way to corroborate click → signup → download unlock.

**Download picker (2.0.5, Aug 17; still current):** `/download` shows platform choices **before** starting a download. `download_artifact_click` `surface` values are `platform_picker` (primary buttons), `platform_switch` (need a different build), and `filename` (filename links). Older Insights rows may still say `autodetect`. Do not compare this week’s `surface` mix to last week without that caveat.

**Corroboration (once numbers exist):**

- `waitlist_click` vs `download_nav_click` → survey-only vs people who already want the DMG. After the download gate, **both** paths can create a hub waitlist row.
- `download_nav_click` ≫ `download_artifact_click` → drop-off on the five-question form (or cookie already set and they bounced before the picker).
- `download_artifact_click` `platform=windows` share vs waitlist Windows 33% → now that the picker shows Windows, are those 3 signups grabbing **2.0.10**?
- `platform_picker` vs `platform_switch` vs `filename`: if most downloads are `platform_picker` on the suggested OS, the chooser is doing its job.
- `sign_in_click` / `create_account_click` vs waitlist n → how many browsers become accounts vs stay on the survey.
- 7d `$mp_web_page_view` vs last-7-day waitlist n → if traffic is alive and waitlist is still 0 after Aug 8, the form (or the LinkedIn hold) is the choke, not “nobody visits.”

### Desktop / Mac+Windows app (`surface=desktop`, `platform=macos|windows`)

There is no separate mobile Mixpanel surface in this repo. The Flutter app registers `surface=desktop`. Use this section for laptop/desktop usage. People may say “the mobile app” when they mean the phone companion — **phone is not emitting yet**.

**Event names (correct these in Insights):** first-run handshake events are `desktop_handoff_*`, not `desktop_ping_*`. Earlier weekly drafts asked for the ping names; those events are not emitted.

Onboarding funnel (no mail content):

`desktop_app_open` → `desktop_sign_in_*` → `desktop_create_team_*` / `desktop_join_invite_*` → `desktop_connect_mcp_*` / `desktop_connect_skill_*` → `desktop_handoff_{picked,copy,open,waiting,success,timeout}` → `desktop_handoff_next` → `desktop_home_ready` / `desktop_tab_select`

Plus **`desktop_update_required`** (`current`, `latest`) when the published-alpha gate fires. 2.0.4 prompted a reinstall when behind; 2.0.5–2.0.6 stopped that check from hanging startup; 2.0.7 disabled the blocking gate, then the tree restored a **non-blocking** gate. **2.0.10** still shows Update required after startup when behind. A spike here on `current=1.1.4`, `2.0.0`, or `2.0.7` would mean people have not yet taken the onboarding cut.

**Dead event names (defined, never `track`ed):** `desktop_connect_host_picked` and `desktop_connect_complete`. Do not treat zero here as “nobody picked a host.” MCP/skill success vs failure is the live connect signal.

**`desktop_handoff_next` `kind` (published 2.0.10):** `work` and `physics` are first-job cards after a successful handshake; `invite` is the solo-org teammate invite; `skip` is **Finish** after success (it is not “user skipped the handshake”). Querying `skip` as drop-off will overstate churn. These kinds fire on **2.0.10**; **2.0.7** installs will not emit them.

`desktop_tab_select` values: `threads` · `collab` · `network`. Collab 2.0 (dashboard, dossiers, brain pane) did **not** add new event names — `tab=collab` is the only in-repo signal that people opened that surface.

**Corroboration:**

- Roy’s MCP friction → watch `desktop_connect_mcp_failure` vs success; if failures cluster, C (Show HN) and any public launch stay blocked.
- Ping wizard as day-one success → `desktop_handoff_success` vs `desktop_handoff_timeout`. On 2.0.10, `desktop_handoff_next` `kind=work|physics` vs Finish (`skip`) shows whether people start a real job or bail to home.
- Waitlist “multiple/day multi-host” → after identify, do the same people `home_ready` more than once in 7d?
- Windows waitlist vs `desktop_app_open` `platform=windows` after 1.1.3 / 2.0.10. The picker is live; Mixpanel should now be able to show whether anyone took it.
- Did anyone update after the 2.0.10 cut (Aug 20)? Split `app_version` 1.1.4 vs 2.0.0 / 2.0.1 vs 2.0.4–2.0.7 vs **2.0.10**. A jump in `desktop_handoff_next` `kind=work|physics|invite` only on 2.0.10 would corroborate that the onboarding cut is being seen.
- `desktop_update_required` vs later `desktop_app_open` on a newer `app_version` → did the prompt actually move people.
- Hub census `replied_threads` vs Mixpanel `desktop_handoff_success` — ops copy already notes Mixpanel still splits work handoff vs ping; use `desktop_handoff_next` `kind` for that split on 2.0.10.

**Coverage caveat:** Mixpanel desktop only exists from **1.1.4 (Aug 13)**. Quiet Insights is not “no usage” for older builds. **2.0.10** is the first published `app_version` that includes first-job / invite kinds.

### Mobile app (iOS companion)

iOS is **v1.5** and has **no Mixpanel events** in this repo. Feedback types already allow `platform=ios`, so in-app feedback is the only in-repo mobile signal until a tracker ships.

If a separate consumer iOS Mixpanel project exists, send the project id + a 7d Insights export so the next run can corroborate website vs phone vs desktop. Until then, **website + desktop** are the only instrumented surfaces.

---

## Product shipped (context for behavior)

Published site / app version is **2.0.10** (bumped 2026-08-20). First-job cards and the four-step onboarding are no longer “unreleased on main.” No newer cut landed on 2026-08-21 or 2026-08-22.

- **1.0.11–1.0.12 (Aug 7):** crash reporting, Windows installer, app/core version match.
- **1.1.0 (Aug 11):** Network tab, external contacts + PIN, compose in toolbar.
- **1.1.1–1.1.2 (Aug 11–12):** `@i` mark, multi-device send reliability (Roy called out UI polish).
- **1.1.3 (Aug 12):** Windows Auth0 sign-in URL fix — relevant to the 3 Windows waitlist rows.
- **1.1.4 (Aug 13):** Network inspect + mail split; **desktop Mixpanel funnel**; web identify stitch.
- **2.0.0 (Aug 16):** Collab tab — boards, steerers, standing instructions; MCP list/open/move.
- **2.0.1 (Aug 16):** Collab on Windows + sync stability.
- **2.0.4 (Aug 17):** Collab home dashboard (dossiers, activity, shared brain); manage members/roster/archive; notifications panel; reinstall prompt when behind published alpha.
- **2.0.5 (Aug 17):** Startup no longer hangs if mutande.online is unreachable; **download page shows platform choices before starting**.
- **2.0.6 (Aug 17):** Update check is non-blocking — app opens immediately.
- **2.0.7 (Aug 17):** Disable blocking startup update gate; tree then restored a non-blocking gate.
- **2.0.10 (Aug 20):** Redesigned onboarding (sign in → team → connect host → first handshake); handshake wizard (pick who replies, open host composer, Finish only after the other agent responds); first-job cards (`work` / `physics`) + invite when solo; Windows installer version-matched; Update required after startup when behind.

**Site confirm (2026-08-22):** `/api/desktop-version` reports Mac + Windows **2.0.10**. Unlocked `/download` renders Silicon / Intel / Windows buttons and filename links (`mutande-alpha.dmg`, `mutande-alpha-intel.dmg`, `mutande-alpha-windows-setup.exe`).

Desktop Mixpanel `app_version` should read **2.0.10** for anyone who installed after the latest cut; older installs stay on 1.1.4 / 2.0.0–2.0.7 until they update.

---

## Inferred behavior (and what Mixpanel must confirm)

| Inference | From | Confirm with |
|-----------|------|----------------|
| Pain is copy/paste + multi-host, not Slack | Waitlist methods; Roy “tab loop” | Web waitlist vs download; desktop handoff + tab use after install |
| Mac-first invite batch is still the safer default | 67% macOS waitlist; v1 Mac-complete | `download_artifact_click` + `desktop_app_open` by platform |
| Windows demand is real; picker is now live | 3 Windows signups; prod `winZipPublished=true` | Windows `download_artifact_click` + `desktop_sign_in_success` on 2.0.10 |
| MCP is the public-launch gate | Roy n=1 | `desktop_connect_mcp_*` (need more than founder/Roy). Ignore `desktop_connect_host_picked` / `_complete` — they never fire |
| Waitlist may have gone quiet after Aug 8 | Snapshot date range | Ops last-7-day waitlist + `waitlist_click` / `download_nav_click` 7d |
| Try Alpha now *is* a waitlist event | `/waitlist?next=/download` gate | Hub completions vs those two click events; form submit still untracked |
| Collab 2.0 is a new desktop surface, not a new funnel | 2.0.0–2.0.4 notes; no new collab event names | `desktop_tab_select` `tab=collab` on `app_version` 2.0.x |
| Stale installs may be stuck pre-2.0.10 | 2.0.4–2.0.10 update-gate churn | `desktop_update_required` + later `app_version=2.0.10` |
| Download chooser replaced silent autodetect | 2.0.5 picker-first UI | `download_artifact_click` `surface=platform_picker` vs older `autodetect` |
| First-job cards are in the published DMG | 2.0.10 changelog + ping wizard | `desktop_handoff_next` `kind=work\|physics\|invite` on `app_version=2.0.10`; `skip` is Finish, not drop-off |
| Hub traffic web can corroborate Mixpanel | `/admin/ops` census graph | `replied_threads`, `independent_self_users`, `star_share` vs desktop handoff success — numbers only, no labels |
| Enterprise is a call topic, not a form segment | Roy; no compliance fields | Do not overweight B until pilots 2–5 or a design-partner ask |
| Mobile usage cannot be corroborated yet | No iOS Mixpanel; feedback `platform` includes `ios` | Ship iOS Mixpanel or export a separate project |

**Strategy score (unchanged without new waitlist):** A high · C high · B low. Stay **A + C**. Do not LinkedIn-blast yet. Windows invites can start as a **small follow-up** (unsigned installer warning), not the primary batch.

---

## Checklist

- [ ] Owner: redacted waitlist + feedback + census snapshot on this PR (PRs #7/#8/#9/#12/#14/#15/#16/#17 can close once this lands)
- [ ] Owner: Mixpanel 7d/30d for **website** + **desktop app** events (or Query secrets for next run). Use `desktop_handoff_*`, not `desktop_ping_*`. Split `app_version` including **2.0.10**
- [ ] Owner: confirm whether any iOS / `surface=mobile` Mixpanel project exists — send project id + 7d Insights if yes
- [x] `/download` currently offers Windows + Intel (`winZipPublished` / `macIntelPublished` true on prod, 2026-08-22)
- [ ] Pilots 2–5 scheduled / filed under `pilot/sessions/YYYY-MM-DD-firstname.md`
- [ ] ≥2-people blocker sprint still waiting on a second MCP/onboarding fail
- [ ] Batch invites still: **macOS + ≥2 hosts + multiple/day** first; Windows waitlist can be a second, warned batch now that the picker shows Windows
- [ ] Roy landing-page workflow clip still open
