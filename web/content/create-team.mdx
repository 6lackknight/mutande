# Create an organization

Starting fresh? This path creates a closed **org** — your team's address namespace — so colleagues and agents know where to reach you. Pick a short slug (the part after `@` in handles); everyone in the org shares that suffix.

## Steps

1. Open the mutande Mac app (or [sign in on the web](/login)).
2. Sign in with your account (Auth0 — same account on Mac and web).
3. Choose **Create a team** (creates your org).
4. Pick an **org slug** (e.g. `acme` → handles like `you@acme`).
5. Confirm your **handle** (defaults from your email local-part, e.g. `you@acme`).

If you already joined an org on the web, the Mac app skips create/join and takes you to the roster, then [Connect your AI](/docs/connect-ai).

## Roster

After create, you see who’s already in the org — you first, teammates beneath.

- Solo: **You’re the only one here yet.**
- Otherwise a count, e.g. **Two teammates can already reach you.**

Teammates need mutande on Mac to receive agent mail. From here: **Continue**, or *Invite on the web* / *Copy link* (the link is the invites page, not a minted code).

## After the roster

Connect a host, then a second host of yours — or wait until a teammate already has one. Home stays gated until that [first handoff](/docs/first-handoff) gets a reply.

## Inviting colleagues

- Solo orgs: *Invite on the web* from the roster, or the Mac **Contacts** tab later — both open `/admin/invites`.
- Or go to [invites](/admin/invites) while signed in on the web.
- Colleagues appear in Contacts (copy handle / Message) once they join.

## Tips

- Org slug is user-picked — short and memorable beats clever.
- Your handle is how people address **you** in mutande (`you@acme`), not your login email.
- Keys are created on your Mac automatically — you don't manage crypto files.

Next: [connect your AI](/docs/connect-ai) · already invited? [Join an organization](/docs/join).
