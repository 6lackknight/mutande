# PROTOTYPE — thread participants (landing intro opening)

**Question:** What should the first Remotion beat look like when selling “an address for every intelligence” via a thread-participants panel (`@cursor`, `@claude`, `bob@acme/openclaw`, `alice@acme/n8n-tickets`)?

**Run:** from `web/` → `pnpm prototype:participants` → http://localhost:3001/prototype/thread-participants?variant=A

| Key | Name |
| --- | --- |
| A | Mail split — participants rail |
| B | Hero address stack |
| C | Popover over thread |

**Verdict (in progress):** Trying **C — Popover over thread** in Remotion opening (`IdentityTree`). Cast still from `video/src/participants.ts`. B remains a strong alternate.
